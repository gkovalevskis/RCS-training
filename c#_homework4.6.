Definēt int tipa masīvu, kurā būs 10 elementi. Izveidot for ciklu, kurš aizpildīs šo masīvu ar skaitļiem. 
Masīvā ir jāieraksta for cikla mainīgā vērtība.


using System;

namespace Uzdevums4._6
{
    class Program
    {
        static void Main(string[] args)
        {
            //Definēt int tipa masīvu, kurā būs 10 elementi. 
            //Izveidot for ciklu, kurš aizpildīs šo masīvu ar skaitļiem. 
            //Masīvā ir jāieraksta for cikla mainīgā vērtība.

            int[] elementi = new int[10];
            


            for (int a = 0; a < elementi.Length; a++)
            {
                elementi[a] = a;

                Console.WriteLine(elementi[a]);

            }
           
        }
    }
}
