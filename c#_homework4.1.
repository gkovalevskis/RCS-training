Izveidot console aplikāciju, kurā ir definēts string tipa masīvs ar cilvēku vārdiem. Izmantojot do while ciklu izdrukāt visus vārdus.


﻿using System;

namespace Uzdevums4._1
{
    class Program
    {
        static void Main(string[] args)
        {
            //Izveidot console aplikāciju, kurā ir definēts string tipa masīvs ar cilvēku vārdiem. 
            //Izmantojot do while ciklu izdrukāt visus vārdus.

            string[] vardi = new string[] { "Aldis", "Boris", "Daniels", "Elvis", "Gunars", "Helmuts" };
            int x = 0;

            do
            {
                Console.WriteLine(vardi[x]);
                x++;
                
            } while (x < vardi.Length);


        }
    }
}
