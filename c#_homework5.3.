Ir dots int tipa masīvs ar sākotnējām vērtībām 4, 68, 7, 12, 56, 33, 1, 90, 4. 
Ir jāatrod visas vērtības, kuras lielākas par 15 un jāievieto JAUNĀ int tipa masīvā, kura saturs ir jāizvada uz ekrāna.


using System;

namespace Uzdevums5._3
{
    class Program
    {
        static void Main(string[] args)
        {
            //Ir dots int tipa masīvs ar sākotnējām vērtībām 4, 68, 7, 12, 56, 33, 1, 90, 4.
            //Ir jāatrod visas vērtības, kuras lielākas par 15 un jāievieto JAUNĀ int tipa masīvā, 
            //kura saturs ir jāizvada uz ekrāna.

            int[] masivs = new int[] { 4, 68, 7, 12, 56, 33, 1, 90, 4 };

            int[] newMasivs = new int[masivs.Length];
            int a = 0;

            for (int i = 0; i < masivs.Length; i++)
            {
                if (masivs[i] > 15)
                {

                    newMasivs[a] = masivs[i];
                    Console.WriteLine(newMasivs[a]);
                    a++;

                }

            }
            
        }
    }
}
